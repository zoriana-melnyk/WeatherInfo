import React from "react";

class Weather extends React.Component{
    render()
    {
        return(
            <div>
                Componet with weather info
            </div>

        );
    }
}

export default Weather;